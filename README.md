# VoidHash
### Transmit the hash into the void; extract the plaintext from the silence.  The absolute zero of password cracking.


VoidHash is an experimental, bare-metal cryptographic brute-forcer. Running independently of any operating system as a native UEFI application, it is designed to quantify the exact performance penalty and latency jitter introduced by OS schedulers and context switching during heavy hashing workloads.
